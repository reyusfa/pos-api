const {
  checkUsername,
  checkEmail
} = require('./users');

// test('Check invalid username', () => {
//   const fetchData = async () => {
//     return await checkUsername('');
//   };
//   return expect(fetchData()).resolves.toBe(false);
// });

// test('Email is available', () => {
//   const fetchData = async () => {
//     return await checkEmail('admin@email.com');
//   };
//   return expect(fetchData()).resolves.toBe(true);
// });
